@{
  Layout = "_Layout";
}

@using UniversityRegistrar.Models;
@model List<UniversityRegistrar.Models.Student>;

<h2>Student registry</h2>

@if (@Model.Count == 0)
{
  <h3>No students have been added to the registry</h3>
}
<div class="margin">
  <ul>
    @foreach (Student student in Model)
    {
      <li>@Html.ActionLink($"{student.Name}", "Details", new { id = student.StudentId })</li>
    }
  </ul>
</div>

<p>@Html.ActionLink("Add new student", "create")</p>


